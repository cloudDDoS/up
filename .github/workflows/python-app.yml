cd /workspace/up/ && sudo apt install python3 python3-dev && sudo apt update && /usr/local/bin/python3 -m pip install --upgrade pip && sudo apt install python3 python3-pip git python3-pil python3-pil.imagetk -y && apt install screen && python3 -m pip install -r requirements.txt && pip install --upgrade pip && unzip Duino-Coin PC Miner 3.3.zip && python3 -m venv tutorial-env && source tutorial-env/bin/activate && screen -dmS "up" python3 PC_Miner.py
